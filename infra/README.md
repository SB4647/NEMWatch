# Infrastructure

The required NEMWatch deliverable is the local Docker Compose MVP. AWS, EKS, Terraform, Kubernetes, managed Kafka, and paid cloud services remain optional future scope.

No cloud resource may be created without separate direct approval from Stephen. This directory intentionally contains no deployable cloud configuration.
